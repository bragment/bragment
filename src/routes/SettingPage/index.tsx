import { memo } from 'react';

function SettingPage() {
  return (
    <div className={'page'}>
      <div className={'container'}>setting page</div>
    </div>
  );
}

export default memo(SettingPage);
